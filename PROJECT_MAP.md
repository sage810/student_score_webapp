# PROJECT_MAP — student score webapp

이 프로젝트에 어떤 파일/폴더가 있고 각각 뭘 하는지만 적는다. 왜 그렇게 만들었는지(판단 이유)는 여기 적지 않는다 — 그건 `.design/decisions.log` 몫이다. 새 파일을 만들거나 지울 때마다 그 즉시 한 줄씩 갱신한다. 나중에 몰아서 정리하지 않는다.

## 구조

```
student score webapp/
├── Code.gs                 서버(Apps Script)
├── appsscript.json         Apps Script 설정
├── index.html              학생 화면
├── teacher.html            교사 화면
├── 수행평가_구글시트_*.xlsx  시트 예시 파일 3개
├── .design/                확정된 디자인 값 (style-lock.md, design-tokens.css/json)
└── .claude/                작업 기록 (AGENTS_MAP.md, pending-changes.log)
```

## 파일별 설명

| 경로 | 역할 |
|---|---|
| Code.gs | 구글 시트와 화면 사이를 잇는 Apps Script 서버 코드. 시트 읽기·쓰기와 교사 비밀번호 검증을 맡고, 점수·결시명단은 과목마다 별도 시트로 나눠 저장함. 홈 화면 통계용 getHomeStats, 결시 칸에 점수를 직접 입력했을 때 결시명단 시트도 맞춰 지우는 removeResolvedAbsences_ 추가됨 |
| appsscript.json | Apps Script 프로젝트 설정 파일 |
| index.html | 학생용 화면. 학년·반·번호·이름으로 조회하고 결과를 표로 보여줌. 과목이 여러 개면 과목별 카드를 옆으로 넘겨 봄. 반 드롭다운도 번호처럼 항상 아래로만 열리는 커스텀 콤보로 바뀜 |
| teacher.html | index.html과 같은 색·글꼴을 쓰지만, 비밀번호 인증 뒤 명단·과목·영역·점수·결시·결시자 수행안내(PDF)·엑셀 다운로드를 관리하는 교사용 화면. 저장하지 않은 변경이 있으면 로그아웃·창 닫기 때 경고함. 로그인 직후 8개 섹션을 바로 펼치는 대신 통계 3개(맡은 과목·결시 대기·이번 수행 완료)와 준비/진행/확인·출력 3단 카드 그리드로 된 홈 화면이 먼저 뜨고, 상단 고정바 "홈" 버튼으로 돌아올 수 있음. "과목" 드롭다운은 항상 아래로 열리는 커스텀 콤보로 통일. 반별 점수 입력표는 결시 칸도 직접 점수 입력 가능(입력 시 결시 자동 해제), 배점 초과 시 저장 차단 + 배지 안내, 30% 이하 저배점은 확인창으로 재확인. "구글 시트에서 열기" 버튼은 하단 고정바(삭제됨)에서 상단 고정바 로그아웃 버튼 왼쪽으로 이동, 둘 다 첫 화면 "확인" 버튼과 같은 보라색 채움 스타일. 상단 탭 메뉴는 전체화면 1줄 → 좁아지면 5열(2행) 그리드 → 더 좁아지면 3열(3행) 그리드로 반응형 |
| 수행평가_구글시트_데모데이터.xlsx | 구글 드라이브에 올려 구글 시트로 바꿔 쓰는 시트 예시(과목·학생·점수가 채워진 체험용) |
| 수행평가_구글시트_빈템플릿.xlsx | 위 파일과 같은 시트 구성에서 머리글·설정값만 남긴 실사용 시작용 |
| 수행평가_구글시트_정보.xlsx | [확인 필요] — "정보" 과목 관련 시트로 보이나 정확한 용도·생성 경위 불명 (이번 세션에서 만든 파일 아님, git에 untracked 상태로 발견됨) |
| .design/style-lock.md | 확정된 색·글꼴·레이아웃 값 (design-system 스킬이 관리) |
| .design/design-tokens.css | style-lock.md와 같은 값을 CSS 변수로 옮긴 것. 다른 프로젝트에 붙여넣어 쓰는 용도 (화면 파일에는 아직 연결 안 됨) |
| .design/design-tokens.json | design-tokens.css와 같은 값을 JS·Figma 등에서 읽기 쉬운 JSON으로 정리한 것 |
| .claude/AGENTS_MAP.md | 이 프로젝트에서 쓴 스킬 기록 |
| .claude/pending-changes.log | 작업 중 남기는 변경 메모 (정리 후 비움) |
